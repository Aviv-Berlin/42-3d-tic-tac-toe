server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location /v1/ {
        proxy_set_header Host $host;
        proxy_pass http://backend:3001;
    }
}
